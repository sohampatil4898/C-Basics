#include<stdio.h>
 int main(){
    int length,width;
    printf("enter the length:");
    scanf("%d",&length);
    printf("enter the width:");
    scanf("%d",&width);
    printf("Area of rectangle:%d",length*width);
    return 0;
 }
 